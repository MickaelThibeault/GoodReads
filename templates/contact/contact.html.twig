{% extends 'base.html.twig' %}

{% block body %}

    <h1 class="h3 mb-3 font-weight-normal">Nous contacter</h1>

    {{ form_start(form) }}

    <div class="flex">
        <div class="flex-1">{{ form_row(form.name) }}</div>
        <div class="flex-1">{{ form_row(form.email) }}</div>
    </div>

    {{ form_end(form) }}

{% endblock %}


{% block title %}
Nous contacter
{% endblock %}

